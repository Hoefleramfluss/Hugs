import React from 'react';

const PagePreview: React.FC = () => {
    return (
        <div className="p-8">
            <h1 className="text-2xl font-bold">Page Preview</h1>
            <p>This is where the built page would be rendered for preview.</p>
        </div>
    );
};

export default PagePreview;
