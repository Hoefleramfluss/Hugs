import React from 'react';
import Footer from '../../Footer';

// This section component might just render the standard site footer,
// or it could be made configurable via props in a more advanced implementation.
const FooterSection: React.FC = () => {
  return <Footer />;
};

export default FooterSection;
